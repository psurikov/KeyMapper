<UserControl
    x:Class="KeyMapper.Views.KeyMappingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:views="clr-namespace:KeyMapper.Views"
    xmlns:controls="clr-namespace:KeyMapper.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <views:KeyComboSeriesView Grid.Column="0" DataContext="{Binding Source}" HorizontalAlignment="Right"/>
            <Label Grid.Column="1">------></Label>
            <views:KeyComboSeriesView Grid.Column="2" DataContext="{Binding Target}" HorizontalAlignment="Left" />
        </Grid>
        <StackPanel x:Name="ButtonPanel" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <Button Content="up" Command="{Binding DataContext.SelectedProfile.MoveUpMappingCommand, RelativeSource={RelativeSource AncestorType={x:Type views:MainWindow}}}"/>
            <Button Content="down" Command="{Binding DataContext.SelectedProfile.MoveDownMappingCommand, RelativeSource={RelativeSource AncestorType={x:Type views:MainWindow}}}"/>
            <Button Content="edit" Command="{Binding DataContext.SelectedProfile.ModifyMappingCommand, RelativeSource={RelativeSource AncestorType={x:Type views:MainWindow}}}"/>
            <Button Content="del" Command="{Binding DataContext.SelectedProfile.RemoveMappingCommand, RelativeSource={RelativeSource AncestorType={x:Type views:MainWindow}}}"/>
        </StackPanel>
    </Grid>
</UserControl>
